p1 is a c file that when execute will run a simple shell interpreter.

Use command (make) to complie 
(./p1) to run the file

To run command in the background of the p1 program.
	type (bg [*+command to run*])
To exit the shell. Type (exit)